export function Usuario(idUser, username, name, surname, sex, transport) {
    this.idUser = idUser;
    this.username = username;
    this.name = name;
    this.surname = surname;
    this.sex = sex;
    this.transport = transport;
}